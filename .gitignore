logs/
deployments/
.env
data/*.db
__pycache__/
*.pyc
sorriva.sqlite
.DS_Store
shared

# SwiftPM build artifacts for the FastTests package (180MB, regenerated by `swift test`)
ios/FastTests/.build/

# Xcode per-user state — window layout, breakpoints, scheme ordering.
# Rewritten on every click, which is what broke `deploy` mid-rebase on 2026-08-08.
xcuserdata/
